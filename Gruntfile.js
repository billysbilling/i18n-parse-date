module.exports = function(grunt) {
    grunt.initConfig({
        'billy-builder': {
            title: 'i18n-parse-date',
            jshint: true
        }
    });

    grunt.loadNpmTasks('billy-builder');
};